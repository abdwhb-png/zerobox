# Fork-local release policy: no automatic publishing for this repo.
# This fork is Linux/WSL2 local-only and does not publish npm / crates.io / PyPI artifacts.

name: Release (disabled)

on: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Release automation is disabled for this fork (no publish targets)."
